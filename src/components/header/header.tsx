import '../../App.css';
import React from "react"
import Logo from "./logo"
function Header() {
    return (
        <header className="header flex">
            <Logo/>
        </header>
    );
}

export default Header;
